# Athlete components

Composed components that render a single athlete card and its form fields.
May import from src/components/ui, src/types, src/hooks.

Rules:
- Keep one component per file.
- Do not put calculation logic here — call hooks or lib functions instead.
- Collapsible state must not clear form data on collapse.
