# Generic utilities

Small, stateless helper functions used across the project.
Examples: formatNumber, cn (className merger), parseDecimal.

Rules:
- No React imports.
- Each utility must be a named export.
- parseDecimal must accept both comma and dot as decimal separator.
